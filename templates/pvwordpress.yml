apiVersion: v1
kind: PersistentVolume
metadata:
  name: wp-pv
  labels:
    app: wordpress
spec:
  capacity:
   storage: 1Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /home/pvwordpress
